

Руководство по T-FLEX CAD Open API

# FragmentVariableValueIsSet - свойство  
    
Задано ли значение переменной. Если значение не задано, то оно определяется значением переменной внутри документа фрагмента.

**Пространство имён:** [TFlex.Model](N_TFlex_Model.md)**Сборка:** TFlexAPI (в TFlexAPI.dll) Версия: 17.1.20.0

```csharp
public bool IsSet { get; set; }
```
```vb
Public Property IsSet As Boolean
	Get
	Set
```
```cpp
public:
property bool IsSet {
	bool get ();
	void set (bool value);
}
```


#### Значение свойства

[Boolean](https://learn.microsoft.com/dotnet/api/system.boolean)

#### Ссылки

[FragmentVariableValue - ](T_TFlex_Model_FragmentVariableValue.md)

[TFlex.Model - пространство имён](N_TFlex_Model.md)